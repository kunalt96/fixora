# Fixora

AI-first error tracking and debugging platform.

## Structure

- backend → API + database
- sdk → error tracking SDK
- dashboard → web app

## Goal

Capture errors → analyze with AI → suggest fixes
